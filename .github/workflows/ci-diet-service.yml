name: CI - diet-service

on:
  push:
    branches:
      - dev
    tags:
      - 'v*'
    paths:
      - 'services/diet-service/**'
      - '.github/workflows/ci-diet-service.yml'
      - '.github/workflows/ci-template.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'services/diet-service/**'
      - '.github/workflows/ci-diet-service.yml'
      - '.github/workflows/ci-template.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  call-template:
    uses: ./.github/workflows/ci-template.yml
    with:
      service-name: 'diet-service'
      docker-path: 'services/diet-service'
      environment: ${{ github.event_name == 'pull_request' && 'pr' || (startsWith(github.ref, 'refs/tags/v') && 'prod' || 'dev') }}
    secrets: inherit
